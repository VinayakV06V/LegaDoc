module hashledger

go 1.20

require github.com/hyperledger/fabric-contract-api-go v1.2.1

// This file is a starting point, not a locked dependency tree — nobody in
// this project has run `go mod tidy` against a real Go toolchain yet (none
// was available in the environment this was written in). Run it yourself
// once you have Go installed; it will pull in the exact transitive
// versions (fabric-chaincode-go, protobuf, etc.) that v1.2.1 actually needs
// and may bump this version number if 1.2.1 is no longer current.
