"""
Loads the synthetic 15-case demo dataset referenced in SYSTEM_DESIGN.md's
Environments table ("demo (judging day)" row). Not implemented yet — this is
a placeholder for whoever owns demo-data prep.

Run via: docker compose exec api python /app/../scripts/seed_demo_data.py
(or adjust the path once this actually does something).
"""

if __name__ == "__main__":
    raise NotImplementedError(
        "Seed the 15-case taxonomy dataset here, with Domestic Violence "
        "fully populated as the demo showcase case (see SYSTEM_DESIGN.md, Flow 4)."
    )
