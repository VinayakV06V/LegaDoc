// Domain 8 — Platform / Admin. Org onboarding, DocumentSchema registry,
// AI Parser recognizer mapping, Stage Requirements, retry-chain-write,
// AI-parser audit trail. See SYSTEM_DESIGN.md, Domain 8 — this role
// concentrates nearly every dangerous capability in the system.
//
// TODO: org onboarding form, schema/recognizer config editor, stage
// requirements editor, needs-review queue, AI-parser audit viewer.
export default function PlatformAdmin() {
  return (
    <div>
      <h1>Platform / Admin</h1>
      <p>Not implemented yet — see SYSTEM_DESIGN.md, Domain 8.</p>
    </div>
  );
}
