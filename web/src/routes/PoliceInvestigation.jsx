// Domain 1 — Police & Investigation. See SYSTEM_DESIGN.md for use cases,
// endpoints touched, and the org-vs-role note (specialized units are roles
// inside the Police org, not separate tenants).
//
// TODO: FIR registration form, IO dashboard (evidence requests, document
// upload, case diary), assign/reassign IO, arrest recording.
export default function PoliceInvestigation() {
  return (
    <div>
      <h1>Police & Investigation</h1>
      <p>Not implemented yet — see SYSTEM_DESIGN.md, Domain 1.</p>
    </div>
  );
}
