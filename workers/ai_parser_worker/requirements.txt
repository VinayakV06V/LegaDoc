celery==5.4.0
redis==5.0.8
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
# presidio-analyzer and spacy: pin exact versions after the first successful
# install and test — deliberately left unpinned-to-patch here rather than
# guess a patch number, since getting this wrong silently breaks the build.
presidio-analyzer>=2.2,<3.0
spacy>=3.7,<3.8
# After install: python -m spacy download en_core_web_lg
